/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package test.job.app;

import test.job.list.LinkedList;

import static test.job.utilities.StringUtils.join;
import static test.job.utilities.StringUtils.split;
import static test.job.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
